{
  "name": "vue-clamp-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run build -r",
    "check": "vp check",
    "test": "vp test",
    "test:browser": "vp test -c vite.browser.config.ts",
    "ready": "vp check && vp test && vp run test:browser && vp run build -r",
    "dev": "vp run website#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "playwright": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
