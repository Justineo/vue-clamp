# Docs Site Documentation Pass

## Goal

Improve the docs site so it helps users choose the right component quickly and understand the practical API constraints without leaving the page.

## Plan

1. Add a compact comparison section between installation and interactive docs to explain when to use `LineClamp` vs `InlineClamp`.
2. Upgrade the example snippets so they reflect real usage, especially `LineClamp` expansion and `InlineClamp` semantic splitting.
3. Add concise API notes for each component that surface the important behavioral constraints already documented in the README.
4. Validate with `vp check`, `vp test`, and `vp run test:browser`.
