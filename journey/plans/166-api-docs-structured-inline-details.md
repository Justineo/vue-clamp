## Goal

Use structured inline documentation for the parts of the API that need more than one sentence: generic `T`, slot props, and the `split()` return shape.

## Plan

1. Replace dense prose for `T`, slot props, and `split()` return fields with compact structured rows inside the relevant API entries.
2. Add minimal styling for those structured rows so they read clearly without introducing a heavier layout.
3. Format the updated docs and re-run the demo browser test.
