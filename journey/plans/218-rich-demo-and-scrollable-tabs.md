# Plan

## Goal

Make the website better demonstrate `RichLineClamp` and keep the component tabs usable on narrow
viewports.

## Steps

1. Expand the `RichLineClamp` demo into a richer, editable example with preset inline-HTML
   content.
2. Make the component tabs horizontally scrollable so touch swipe and manual horizontal scrolling
   work when the tabs no longer fit.
3. Add browser coverage for the richer rich-text demo behavior and the narrow-width tabs overflow.
4. Run validation and update `journey/design.md` plus the session log.

## Outcome

- Done on 2026-04-08.
- The website now has an editable, preset-driven `RichLineClamp` demo instead of a single static
  sentence.
- The component tabs now use horizontal overflow on narrow widths so they stay readable and
  swipeable.
