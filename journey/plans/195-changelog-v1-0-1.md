# 195 Changelog v1.0.1

## Goal

Add a `1.0.1` changelog entry that reflects the current uncommitted release scope in a simple,
user-facing style.

## Steps

1. Inspect the uncommitted diff to separate library-facing fixes from test-only maintenance work.
2. Add a `1.0.1` entry to `CHANGELOG.md` using the repo's simplified release-note style.
3. Record the changelog decision in the matching journey log.
