# Hero Use-Case Tagline

## Goal

- Replace the hero's component-taxonomy animation words with straightforward real-world use cases.
- Keep the current runtime measurement and width animation model intact.

## Steps

1. Update the hero tagline pool and sentence structure in `packages/website/src/App.vue`.
2. Remove the old hardcoded `line` / `inline` / `wrap` defaults from the animation logic.
3. Align the hidden accessibility copy with the new use-case framing.
4. Run focused website browser validation.
