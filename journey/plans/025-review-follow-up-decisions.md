# Review Follow-up Decisions

## Goal

Update the triage decisions after follow-up product guidance on three review items:

- `1.1` legacy middle-mode behavior should be fixed if it is a real bug
- `2.4` first-mount unclamped paint should be reconsidered
- `6.3` benchmark code should not live under `src/`

## Plan

1. Update the project memory to reflect the new decision boundaries.
2. Re-evaluate the three items against the current implementation.
3. Document the concrete implementation direction before making code changes.
