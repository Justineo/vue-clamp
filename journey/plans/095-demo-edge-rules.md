# Demo Edge Rules

## Scope

- `packages/website/src/App.vue`

## Changes

1. Replace the boxed `demo-clamp` surface with a sharper edge-rule treatment.
2. Apply a right-side rule to demo text containers by default.
3. Add a bottom rule for the `max-height` demo variant.
4. Keep the existing browser test selector class.

## Validation

- `vp check`
- `vp run test:browser`
