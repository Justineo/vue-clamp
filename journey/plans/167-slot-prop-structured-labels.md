## Goal

Refine the structured API details so slot props read as output contracts: use a clear `Slot props` label, improve detail-term styling, and remove misleading `required` markers from slot props.

## Plan

1. Restructure slot entries to use a primary `Slot props` detail term with nested structured rows for each prop.
2. Improve the styling for primary detail terms and nested contract rows.
3. Format the docs and run the demo browser test.
