# Plan

## Goal

Add a CSS hyphens toggle to the website `RichLineClamp` demo so the richer article-style sample can
be previewed with or without browser hyphenation.

## Steps

1. Reuse the existing website hyphenation pattern from the text demos instead of adding a separate
   control model.
2. Wire a `CSS Hyphens` toggle into the rich demo and apply it directly to the `RichLineClamp`
   preview root.
3. Update browser coverage plus journey memory, then rerun validation.
