## Goal

Tighten the website documentation so the component reference reads as a single structured system: clearer section grouping, steadier alignment, and fewer low-value notes.

## Plan

1. Review the current docs layout in `packages/website/src/App.vue` and identify noisy or weakly aligned reference sections.
2. Restructure the API area into clearer grouped blocks, improve visual alignment of names and metadata, and trim notes down to essential guidance.
3. Format the updated files and run the demo browser test to confirm the docs page still behaves as expected.
