--- a/tools/benchmark/scripts/measure-update-costs.mjs
+++ b/tools/benchmark/scripts/measure-update-costs.mjs
@@ -37,7 +37,7 @@
 if (![rounds, steps, ...counts].every((number) => Number.isInteger(number) && number > 0))
   throw new Error("Counts, rounds, and steps must be positive integers.");
 const allowedTokens = new Set(
-  "source resize noop font item metrics line inline rich wrap plain distinct large huge short full cjk emoji grapheme longword needle nested atomic affix height tiny".split(
+  "pool2 pool4 pool8 medium source resize noop font item metrics line inline rich wrap plain distinct large huge short full cjk emoji grapheme longword needle nested atomic affix height tiny".split(
     " ",
   ),
 );
@@ -125,7 +125,7 @@
       ? "emoji"
       : "latin";
   const copy = copies[sourceKind],
-    length = scenario.includes("huge") ? 30000 : scenario.includes("large") ? 6000 : 600;
+    length = scenario.includes("huge") ? 30000 : scenario.includes("large") ? 6000 : scenario.includes("medium") ? 1500 : 600;
   const source = scenario.includes("short")
     ? "Short title"
     : scenario.includes("longword")
@@ -146,7 +146,7 @@
         Array.from({ length: count }, (_, i) => {
           const text =
             (scenario.startsWith("source") ? revision.value + ": " : "") +
-            (scenario.includes("distinct") ? i + ": " : "") +
+            (scenario.includes("pool") ? (i % Number(scenario.match(/pool(\d+)/)[1])) + ": " : scenario.includes("distinct") ? i + ": " : "") +
             source;
           const style = {
             display: "block",
@@ -358,6 +358,7 @@
           );
           let result;
           if (heapMode) {
+            await page.evaluate((steps) => window.run(steps), steps);
             const used = await usedHeap();
             result = {
               emptyHeap,
