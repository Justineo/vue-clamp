# 2026-04-09

- Starting the website "Choose a surface" redesign:
  - replace the plain list with a clearer structured surface guide
  - keep the copy brief and aligned with the four actual component surfaces
  - link each entry to the matching component-tab hash
- Implemented:
  - replaced the old bullet list with a split layout: intro copy plus four structured surface rows
  - reused the surface metadata so guide entries and component tabs stay aligned
  - linked each entry to the corresponding `#line-clamp` / `#rich-line-clamp` / `#inline-clamp` /
    `#wrap-clamp` route hash
  - added browser coverage for the new section structure and links
