# 2026-04-05 Hero Use-Case Tagline

- Reframed the animated hero copy around concrete use cases instead of component taxonomy.
- Kept the existing runtime measurement loop so each use-case word still gets its own measured width.
- Updated the hero sentence to `Keep … compact in Vue.` with the pool `excerpts`, `filenames`,
  `paths`, `tags`, and `filters`.
