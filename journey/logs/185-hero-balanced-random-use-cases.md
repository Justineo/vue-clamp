# 2026-04-05 Hero Balanced Random Use Cases

- Reworked the hero pool into balanced line / inline / wrap categories.
- Randomized the sequence by shuffling within each category and then interleaving rounds.
- Visible pool:
  - line: `messages`, `excerpts`, `summaries`
  - inline: `filenames`, `emails`, `paths`
  - wrap: `tags`, `filters`, `chips`
